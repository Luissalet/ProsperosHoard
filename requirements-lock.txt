# Exact set to `pip install -r requirements-lock.txt` into the repo's own
# .venv. Generated by installing pyproject.toml's dependencies (+ the piper
# extra) into a fresh Python 3.11 venv on Linux and freezing. All of these
# ship cp311/cp313 win_amd64 wheels except where noted.
annotated-types==0.8.0
anyio==4.15.1
certifi==2026.7.22
click==8.5.0
fastapi==0.115.6
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0 ; sys_platform != "win32"
httpx==0.28.1
httpx-sse==0.4.3
idna==3.20
imageio-ffmpeg==0.5.1
iniconfig==2.3.0
mcp==1.9.4
numpy==2.2.1
packaging==26.3
pillow==11.1.0
pluggy==1.6.0
pydantic==2.10.4
pydantic-settings==2.15.0
pydantic_core==2.27.2
pytest==8.3.4
pytest-asyncio==0.25.0
python-dotenv==1.2.3
python-multipart==0.0.20
PyYAML==6.0.3
scipy==1.14.1
typing-inspection==0.4.4
sse-starlette==3.0.3
starlette==0.41.3
typing_extensions==4.16.0
uvicorn==0.34.0
uvloop==0.22.1 ; sys_platform != "win32"
watchfiles==1.3.0
websockets==17.1

# Optional: real Piper TTS (`pip install -r requirements-lock.txt` does not
# install this group; run `pip install piper-tts==1.8.0` separately, or add
# `.[piper]` when installing from pyproject.toml). Ships a cp39-abi3 wheel,
# so it covers 3.9-3.13 including Windows win_amd64.
# piper-tts==1.8.0
# onnxruntime==1.30.0
# flatbuffers==25.12.19
# pathvalidate==3.3.1
# protobuf==7.36.2

# -e . (install the project itself in editable mode: pip install -e .)
